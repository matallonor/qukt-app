import { NgModule } from '@angular/core';

@NgModule({
	declarations: [
	  // Declare components we want to share
  ],
	imports: [],
	exports: [
    // Export components we want to share
  ]
})
export class ComponentsModule {}
